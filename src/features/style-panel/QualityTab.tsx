/**
 * STYLE panel "Quality" tab. The reference mockup's Quality tab content is
 * cut off in the screenshot, so rather than invent settings with no spec
 * behind them, this is an honest placeholder.
 */
export function QualityTab() {
  return (
    <div className="style-tab" data-testid="quality-tab">
      <p className="style-tab__placeholder">More controls coming soon.</p>
    </div>
  )
}
